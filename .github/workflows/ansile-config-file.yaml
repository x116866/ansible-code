name:  Configuration ansible.cfg 

on:
  workflow_dispatch:

jobs:
  generate-ansible-cfg:
    runs-on: [ ansible-master ]

    steps:


      - name: Generate ansible.cfg
        run: |
          sudo -u videl bash -c 'cat > /home/videl/ansible/ansible.cfg' << 'EOF'
          [defaults]
          inventory      = /home/videl/ansible/inventory.yaml
          roles_path    = /home/videl/ansible/roles
          host_key_checking = False
          remote_user = videl
          private_key_file = /home/videl/.ssh/id_rsa
          log_path = /home/videl/ansible/ansible.log
          forks = 5
          gathering = smart
          fact_caching = jsonfile
          fact_caching_connection = /home/videl/ansible/facts_cache
          

          [privilege_escalation]
          become = True
          become_method = sudo
          become_user = root
          become_ask_pass = False

          [ssh_connection]
          ssh_args = -o ControlMaster=auto -o ControlPersist=60s -o ConnectTimeout=10
          control_path = /home/videl/.ssh/id_rsa-%%r@%%h:%%p
          EOF
          

      - name: Set proper permissions
        run: |
          sudo chown videl:videl /home/videl/ansible/ansible.cfg
          sudo chmod 644 /home/videl/ansible/ansible.cfg

      - name: Verify configuration
        run: |
          echo "Ansible configuration:"
          sudo -u videl ls -la /home/videl/ansible/ansible.cfg
          echo -e "\nContents:"
          sudo -u videl cat /home/videl/ansible/ansible.cfg
